Before start - check the autopilot Enrollment if okay? 

#only get serialnumber
Get-CimInstance Win32_BIOS | Select-Object SerialNumber
# quick one-liner:  (Get-CimInstance Win32_BIOS).SerialNumber



====


(Get-CimInstance Win32_BIOS).SerialNumber   # verify = PF3BR9R7

# Correct reset commands:
systemreset          # opens "Reset this PC" (interactive - pick "Remove everything")
# or
systemreset -factoryreset    # jumps straight to the "Remove everything / Keep files" chooser
